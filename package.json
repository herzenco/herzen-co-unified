{
  "private": true,
  "scripts": {
    "sync-content": "node --env-file-if-exists=.env scripts/sync-content.mjs",
    "test": "node --test tests/*.test.mjs",
    "prepare-public": "node --env-file-if-exists=.env scripts/prepare-public.mjs",
    "build": "npm run sync-content && npm test && npm run prepare-public"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "marked": "^16.4.2",
    "sanitize-html": "^2.17.6"
  }
}
