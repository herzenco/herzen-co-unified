{
  "private": true,
  "scripts": {
    "sync-content": "node scripts/sync-content.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "npm run sync-content && npm test"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "marked": "^16.4.2",
    "sanitize-html": "^2.17.6"
  }
}
